1. 登陆服务器所需私钥，根据id，对应的mig\${id}私钥进行登陆。将私钥保存一个文本文件在执行ssh命令的目录，命名为mig\${id}
2. 用户名为mig${id},如id为01，则用户名为mig01
3. 服务器ip为39.107.68.147，对应登陆端口是11${id},如果id是01，则端口为1101，以此类推
4. 如果id为01，连接命令为 ssh -i ./mig01 mig01@39.107.68.147 -p 1101 
5. **注意：当前公网服务器流量有限，尽量不要通过服务器上传大文件，禁止使用隧道转发访问互联网**